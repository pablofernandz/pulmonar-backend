# Server
PORT=3000
NODE_ENV=development

# Database (example values)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=pulmonar2
DB_LOGGING=false

# Auth (generate your own secret)
JWT_SECRET=change_me_generate_a_new_secret
JWT_EXPIRES_IN=8h
